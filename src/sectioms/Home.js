import React from "react";
import styled from "styled-components";

const Section = styled.section`
  position: relative;
  min-height: 100vh;
`;

const Home = () => {
  return <div>Home</div>;
};

export default Home;
